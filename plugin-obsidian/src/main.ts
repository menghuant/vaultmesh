// TODO: Obsidian plugin entry point
// Will register sync commands and handle vault file change events

export default class VaultMeshPlugin {
  async onload() {
    console.log("VaultMesh plugin loaded — sync not yet implemented");
  }

  async onunload() {
    console.log("VaultMesh plugin unloaded");
  }
}
