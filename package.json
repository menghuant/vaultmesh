{
  "name": "vaultmesh",
  "private": true,
  "workspaces": [
    "packages/shared",
    "server",
    "daemon"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "bun run --filter '*' build",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
