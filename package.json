{
  "name": "vaultmesh",
  "private": true,
  "workspaces": [
    "packages/shared",
    "server",
    "daemon"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "bun run --filter '*' build",
    "lint": "tsc --noEmit",
    "db:push": "cd packages/shared && bunx drizzle-kit push --config ../../drizzle.config.ts",
    "db:generate": "cd packages/shared && bunx drizzle-kit generate --config ../../drizzle.config.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
